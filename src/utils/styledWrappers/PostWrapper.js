/* import styled from "styled-components"

const MdxWrapper = styled.article`
  p {
    margin-bottom: ${({ theme }) => theme.spaces.margin.big};
    line-height: 1.9;
  }

  blockquote {
    position: relative;
    display: inline-flex;
    margin-bottom: ${({ theme }) => theme.spaces.margin.base};

    ::after {
      position: absolute;
      content: "";
      height: 100%;
      width: 5px;
      top: 0;
      left: 0;
      background-color: ${({ theme }) => theme.colors.primary_opacity};
    }
  }

  ul {
    list-style: disc;
    padding-left: ${({ theme }) => theme.spaces.padding.big};
  }

  li {
    padding: ${({ theme }) => theme.spaces.padding.small};
  }

  strong {
    font-weight: bolder;
  }

  img {
    margin: ${({ theme }) => theme.spaces.margin.xxl} 0;
    padding: ${({ theme }) => theme.spaces.padding.big} 0;
  }

  h2 {
    margin: ${({ theme }) => theme.spaces.margin.base} 0;
    padding: ${({ theme }) => theme.spaces.padding.base} 0;
  }

  h3 {
    color: ${({ theme }) => theme.colors.black};
    font-size: 1.4em;
    font-style: italic;
  }

  a {
    display: inline-block;
    color: #000;
    text-decoration: none;

    :after {
      content: "";
      display: block;
      width: 100%;
      height: 1px;
      background: #0b0a0a;
      transition: width 0.3s;
    }
    :hover::after {
      width: 0%;
    }
  }
`

export default MdxWrapper
 */
