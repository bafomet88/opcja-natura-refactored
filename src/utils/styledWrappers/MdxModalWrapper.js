/* import styled from "styled-components"

const MdxWrapper = styled.div`
  p {
    margin-bottom: ${({ theme }) => theme.spaces.margin.big};
    font-weight: ${({ theme }) => theme.fonts.fontWeight.light};
  }

  blockquote {
    position: relative;
    display: inline-flex;
    background-color: ${({ theme }) => theme.colors.green_opacity_light};
    padding-top: ${({ theme }) => theme.spaces.padding.small};
    padding-bottom: ${({ theme }) => theme.spaces.padding.small};
    padding-left: ${({ theme }) => theme.spaces.padding.big};
    margin-bottom: ${({ theme }) => theme.spaces.margin.small};
    margin-left: ${({ theme }) => theme.spaces.margin.big};
  }

  ul {
    list-style: disc;
    padding-left: ${({ theme }) => theme.spaces.padding.big};
  }

  li {
    padding: ${({ theme }) => theme.spaces.padding.small};
  }

  strong {
    display: block;
    font-weight: ${({ theme }) => theme.fonts.fontWeight.normal};
    font-size: ${({ theme }) => theme.fonts.fontSize.xl};
    margin-bottom: ${({ theme }) => theme.spaces.margin.medium};
    letter-spacing: 1px;
  }

  h3 {
    text-align: center;
    letter-spacing: 2px;
    color: ${({ theme }) => theme.colors.font};
    font-weight: ${({ theme }) => theme.fonts.fontWeight.light};
    padding: ${({ theme }) => theme.spaces.padding.medium} 0;

    @media (min-width: 800px) {
      text-align: left;
    }
  }
`

export default MdxWrapper
 */
